{
  "name": "subgraph-events",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/subgraph-events/src",
  "projectType": "application",
  "tags": ["scope:events", "type:app"],
  "targets": {
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/dist/apps/subgraph-events/package.json",
        "{workspaceRoot}/dist/apps/subgraph-events/package-lock.json"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/dist/apps/subgraph-events/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "subgraph-events:build",
        "runBuildTargetDependencies": false,
        "port": 9231
      },
      "configurations": {
        "development": {
          "buildTarget": "subgraph-events:build:development"
        },
        "production": {
          "buildTarget": "subgraph-events:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/subgraph-events/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}
