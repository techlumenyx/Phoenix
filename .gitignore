# Dependencies
node_modules/

# Build outputs
dist/
build/
out/

# Nx cache
.nx/cache
.nx/workspace-data
.nx/self-healing
.nx/migrate-runs

# Environment secrets — never commit
.env
.env.local
.env.*.local

# Coverage
coverage/

# Apollo supergraph (regenerated by CI — committed only on main)
apps/gateway/supergraph.graphql

# GraphQL codegen output (regenerated — committed once baseline is established)
libs/types/src/lib/generated/graphql.ts
apps/web/src/graphql/__generated__/

# Vite
vite.config.*.timestamp*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.suo
*.user

# Firebase service account (never commit)
serviceAccount.json
*-firebase-adminsdk-*.json

# MCP config (contains secrets — never commit)
.mcp.json

# Apollo Router binary (downloaded locally — not committed)
tools/bin/router.exe
tools/bin/router

# Logs
*.log
npm-debug.log*

# Local database backups
backups/

# Apollo Router binary
router.exe
router

# Local Rover license and plugin metadata
.nx/rover/
